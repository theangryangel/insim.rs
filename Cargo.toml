[workspace]
members = [
  "examples/*",
  "insim",
  "insim_core",
  "insim_pth",
  "insim_smx",
  "outgauge",
]
# Only check / build main crates by default (check all with `--workspace`)
default-members = ["insim", "insim_*", "outgauge"]
resolver = "2"
