services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: cwc
      POSTGRES_PASSWORD: cwc
      POSTGRES_DB: cwc
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
